<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">

    <%= form.label :cuisine_id %>
    <% debugger %>

    <%= form.select :name , options_for_select(@cuisine.map(&:name))%>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :accepts_10bis %>
    <%= form.check_box :accepts_10bis %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :max_delivery_time_minutes %>
    <%= form.number_field :max_delivery_time_minutes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
